# hri-bootstrap — PARKED (not in active use)

> **This is not the current HRI onboarding path.** HRI onboarding uses the
> `hri-claude-code-onboarding` repo (`setup.sh`). Do not point HRI builders here.

**Status:** parked on the punch list. Reserved for repurposing into a **generic, non-HRI**
one-command Claude Code + Nimbalyst installer for training workshops — **Accord-Intensive**
and the **July AI Intensive**, where participants set up Claude Code + Nimbalyst themselves.

**Before reuse it must be genericized:** the current `install.sh` signs into GitHub and the
flow assumes the HRI private onboarding repo. For a workshop audience, strip the HRI-specific
pieces (org membership, the private-repo handoff, the service-account impersonation) down to
a clean public installer that any participant can run.
